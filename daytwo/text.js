function changeText () {
    var parent = document.getElementById("main");
    var child = parent.lastElementChild;

   
    
     /* we do not need to worry about white spaces here. Last: gets the last paragraph. First: gest the 1st paragraph
     
   child.style.color = "blue";
   //var child = parent.childNodes [7];
   /*builds a node list of all the nodes under our parent
    We target the second paragraph here. Note that child node accomodates both the white spaces. Element accomadates only the para.
    */
}